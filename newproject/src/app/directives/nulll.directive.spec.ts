import { NulllDirective } from './nulll.directive';

describe('NulllDirective', () => {
  it('should create an instance', () => {
    const directive = new NulllDirective();
    expect(directive).toBeTruthy();
  });
});
